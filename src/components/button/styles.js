import { makeStyles } from '@material-ui/styles'

export default makeStyles(() => ({
  loading: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    paddingTop: '0.4rem',
  },
}))
